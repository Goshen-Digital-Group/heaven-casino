# --- Heaven Casino Custom Environment Variables ---

# App environment
GAMBA_ENV=production

# Access override password (server-only)
ACCESS_OVERRIDE_PASSWORD=YOUR_ACCESS_OVERRIDE_PASSWORD

# Access override message
VITE_ACCESS_OVERRIDE_MESSAGE=Access is currently restricted. Please check back later!

# Access override settings
VITE_ACCESS_OVERRIDE_ENABLED=true

# Offline message (custom)
VITE_OFFLINE_MESSAGE=We’re catching our breath, stacking sats in the shadows. Back soon to ride the next wave — hold tight, fam.

# Helius API key (server-only)
HELIUS_API_KEY=YOUR_HELIUS_API_KEY

# Syndica RPC endpoint
VITE_RPC_ENDPOINT=YOUR_SYNDICA_RPC_ENDPOINT

# Web3 connection
VITE_PLATFORM_CREATOR=YOUR_PLATFORM_CREATOR

# ChangeNOW API for crypto swaps (server-only)
CHANGENOW_API_KEY=YOUR_CHANGENOW_API_KEY

# Offline message when outside live hours
VITE_OFFLINE_MESSAGE=We'll be back soon!
